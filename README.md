# cv-screener
Demo AI agent that screens resumes against a job description — enforces a code-level no-PII guardrail on its output and a hard iteration cap, so it fails safe instead of leaking candidate data or looping forever.
